# Ultra-minimal requirements for Railway deployment
# Note: PyTorch is installed separately with CPU-only index

# Core web framework (essential)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
requests==2.31.0

# Document processing (essential)
pymupdf==1.23.0
python-docx==1.0.1

# Database (optional but included)
psycopg2-binary==2.9.9

# ML/AI (sentence transformers will install torch dependencies)
sentence-transformers==2.2.2

# Vector database (essential for app functionality)
pinecone==7.3.0

# LLM API (essential)
groq==0.4.1
