# Full requirements with ML dependencies (for future use when build tools are available)
# NOTE: This file includes dependencies that require compilation and may not work on all platforms

# Core web framework (essential)
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
requests==2.31.0

# Document processing (essential)
pymupdf==1.23.0
python-docx==1.0.1

# Database (optional but included)
psycopg2-binary==2.9.9

# Vector database (essential for app functionality)
pinecone-client==2.2.4

# LLM API (essential)
groq==0.4.1

# ML dependencies (requires compilation)
numpy==1.24.3
scipy==1.10.1

# For when you need more ML features:
# sentence-transformers==2.2.2
# scikit-learn==1.3.0
# torch==2.0.1
