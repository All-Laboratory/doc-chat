# Railway deployment - Safe version without system dependencies
# Uses PyPDF2 instead of PyMuPDF to avoid libcrypt.so.2 errors

# Core web framework
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0

# File upload support
python-multipart==0.0.6

# HTTP client for API calls
requests==2.31.0

# System monitoring for health checks
psutil==5.9.6

# Document processing - No system dependencies required
PyPDF2==3.0.1
python-docx==1.0.1

# LLM API (essential)
groq==0.4.1

# Optional (uncomment if needed):
# psycopg2-binary==2.9.9  # Database support
# pinecone-client==2.2.4   # Vector database

# Total estimated size: ~120MB of Python packages
# No system dependencies - Railway safe
