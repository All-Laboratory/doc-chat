# Railway deployment with PDF/DOCX support
# Optimized for Railway's build limits while enabling document processing

# Core web framework
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0

# File upload support
python-multipart==0.0.6

# HTTP client for API calls
requests==2.31.0

# System monitoring for health checks
psutil==5.9.6

# Document processing (essential for PDF/DOCX support)
pymupdf==1.23.0
python-docx==1.0.1

# LLM API (essential)
groq==0.4.1

# Optional (uncomment if needed):
# psycopg2-binary==2.9.9  # Database support (adds ~15MB)
# pinecone-client==2.2.4   # Vector database (adds ~50MB)

# Total estimated size: ~250MB of Python packages
# Should fit comfortably within Railway's limits
