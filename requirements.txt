# Emergency ultra-minimal - absolute smallest possible deployment
# Use this if image size still exceeds 4GB

# Core web framework only
fastapi==0.104.1
uvicorn==0.24.0
python-dotenv==1.0.0

# Absolutely essential for file uploads
python-multipart==0.0.6

# Basic HTTP client
requests==2.31.0

# LLM API (essential)
groq==0.4.1

# Everything else disabled for emergency deployment:
# pymupdf - document processing (adds ~20MB)
# python-docx - document processing (adds ~5MB)  
# psycopg2-binary - database (adds ~15MB)
# numpy - math operations (adds ~30MB)
# pinecone-client - vector database (adds ~50MB)

# Total estimated size with this config: <200MB of Python packages
