# Ultra-minimal requirements for Railway - emergency fallback
# Only includes packages with guaranteed binary wheels

# Core web framework (essential)
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
python-dotenv==1.0.0
requests==2.31.0

# Document processing (essential)
pymupdf==1.23.0
python-docx==1.0.1

# Database (optional but included)
psycopg2-binary==2.9.9

# LLM API (essential)
groq==0.4.1

# No numpy to avoid any potential compilation issues
# Vector database temporarily disabled - can be added back later
# pinecone-client==3.0.0
